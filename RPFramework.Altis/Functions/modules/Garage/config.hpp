class RPF_garageModule {
    switch_mode = 0;
    /*
    Mode 0: Vehicles are not persistent.
    All vehicles will get stored each time the server restarts.
    
    Mode 1: Vehicles are persistent.
    Vehicles won't get stored automatically.
    They'll get spawned to their previous position each server restart.
    */
};